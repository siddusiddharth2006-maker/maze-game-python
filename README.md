# Maze Game Python

A simple maze game built in Python using Pygame.  
Move the blue square (player) from the start (S) to the goal (G) avoiding walls.

## Installation

```bash
pip install -r requirements.txt
python maze_game.py
